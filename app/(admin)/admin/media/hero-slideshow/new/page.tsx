import { SlideEditor } from "../[id]/edit/SlideEditor";

export const metadata = {
  title: "Create Hero Slide — Admin RFC Store",
};

export default function NewHeroSlidePage() {
  return <SlideEditor isNew />;
}
